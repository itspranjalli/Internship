"""EDB RIS(C) grant claim automation POC (ST Engineering AI COE).

Package layout follows PLAN.md §1. The deterministic core (calc/, output/)
must never import the LLM adapter (llm/). See CLAUDE.md for locked decisions
and domain rules, PRD.md for requirements.
"""
